<system name="rotor control">

  <!-- =========================================================================
  =================
    Documentation
  =================

  - Rationale -

     Besides the rotor definition which is handled by the C++ code - obviously
     the most important part of a helicopter model - a good control setup is
     actually more important than the remaining aerodynamics (assuming the
     given/guessed coefficients are at least within a 50% window).

     The basic relation is straight forward:
                rotor-ctrl-x = pilot-input-x * gain + offset.

  - Collective Mixing -

     Unless real-world data is available, it's reasonable to adjust gain and
     offset for minimal input travel as a first step. This simple approach has
     a drawback. When one input is changed, the other three need to be
     readjusted. This is most notably when the collective input is changed, and
     to compensate for this, some part of the collective input is mixed into
     the lateral and yaw channels.

     There are two principal goals of collective-mixing:

       a) compensation in level flight - lateral, and anti-torque controls
          follow accordingly, minimizing input requirements.

       b) compensation for the torque change - lateral, and anti-torque changes
          should give proper counter moments. 

     Both tasks can't be fullfilled with simple contol mixing, the compromise in
     this system is biased towards dynamic compensation of the collective input.

  - Input Mapping -

     The system also provides configurable support for simulator inputs.
     Collective input can be turned more sensitve for the intermediate control
     range (fcs/adj/collective-profile), and the other inputs use a common
     'power function' approach for center sensitivity (fcs/adj/center-sensitivity).
     These features might be also present as part of an advanced joystick
     configuration, so it's your choice which one to use.

  - Notes -

     All adjustable control parameters can be found under 'JSBSIMNODE/fcs/adj/'.
     If the model is run with flightgear, it's relatively easy to optimize the
     parameters via the property tree browser.

     An enabled AFCS/SAS should be able to ease control a lot, and AFAIK all
     larger and all modern helicopters rely on such systems.

                -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -

  ===============================
    System Interface/Properties
  ===============================

   Inputs by the pilot:
      fcs/collective-cmd-norm (often called 'pitch' in germany - a brilliant convention...)
      fcs/elevator-cmd-norm (longitudinal)
      fcs/aileron-cmd-norm (lateral)
      fcs/rudder-cmd-norm (anti-torque, tail-collective)

   Trim inputs/settings:
      fcs/collective-trim-cmd-norm
      fcs/pitch-trim-cmd-norm
      fcs/roll-trim-cmd-norm
      fcs/yaw-trim-cmd-norm

   Inputs from AFCS/SAS system:
      ap/collective-cmd
      ap/elevator-cmd
      ap/aileron-cmd
      ap/rudder-cmd

   Adjustable properties:
      fcs/adj/collective-gain
      fcs/adj/collective-bias

      fcs/adj/longitudinal-gain
      fcs/adj/longitudinal-bias

      fcs/adj/lateral-gain
      fcs/adj/lateral-bias
      fcs/adj/lateral-col-mix-infl
      fcs/adj/lateral-col-mix-gain
      fcs/adj/lateral-col-mix-bias

      fcs/adj/pedal-gain
      fcs/adj/pedal-bias
      fcs/adj/pedal-col-mix-infl
      fcs/adj/pedal-torque-mix-gain
      fcs/adj/pedal-col-mix-bias

   Simulator 'mapping' properties:
      fcs/adj/collective-profile
      fcs/adj/center-sensitivity

   Outputs for a decent helicopter (ie: big main rotor, smaller tail rotor for
   anti-torque):
      propulsion/engine[0]/collective-ctrl-rad
      propulsion/engine[0]/longitudinal-ctrl-rad
      propulsion/engine[0]/lateral-ctrl-rad
      propulsion/engine[2]/antitorque-ctrl-rad

       ========================================================================= -->

  <!-- ==================================
         Declare and initialise control
       ================================== -->

  <property value="0.0"> fcs/collective-cmd-norm </property>
  <property value="0.0"> fcs/collective-trim-cmd-norm </property>

  <!-- ==================================
         Setup for interactive use
       ================================== -->

  <!-- 
      Collective input, provide more 'bits' for the intermediate range, 0 for
      unmodified input, 1 for more sensitivity.
  -->
  <property value="1.0"> fcs/adj/collective-profile </property>

  <!--
      Higher center resolution for the other three controls, set to 1 for
      unmodified input, treat 2 as an upper limit.
  -->
  <property value="1.5"> fcs/adj/center-sensitivity </property>

  <!-- - - - - - - - - - - - - - - - - -
          math helper
       - - - - - - - - - - - - - - - - - -->

  <channel name="collective control profile">
    <fcs_function name="fcs/misc/collective-cmd-norm-polymod">
        <description>
          A s-shaped profile, described with a 5th order polynomial.
          f(x) = x**5 + x | [-1:1], mapped to [0:1]x[0:1], resulting in
          f(x) = 8*x**5 - 20*x**4 + 20*x**3 - 10*x**2 + 3*x.
        </description>
      <function>
        <table>
          <independentVar lookup="row"> fcs/collective-cmd-norm </independentVar>
          <tableData>
            0.000000 0.000000
            0.050000 0.127377
            0.100000 0.218080
            0.150000 0.282983
            0.200000 0.330560
            0.250000 0.367188
            0.300000 0.397440
            0.350000 0.424393
            0.400000 0.449920
            0.450000 0.474997
            0.500000 0.500000
            0.550000 0.525002
            0.600000 0.550080
            0.650000 0.575608
            0.700000 0.602560
            0.750000 0.632813
            0.800000 0.669440
            0.850000 0.717018
            0.900000 0.781920
            0.950000 0.872623
            1.000000 1.000000
          </tableData>
        </table>
      </function>
    </fcs_function>
  </channel>

  <!-- ==================================
         Rotor Control System
       ================================== -->

  <!-- - - - - - - - - - - - - - - - - -
          collective
       - - - - - - - - - - - - - - - - - -->
  <property value='0.25'> fcs/adj/collective-gain </property>
  <property value='0.15'> fcs/adj/collective-bias </property>

  <channel name="collective">

    <fcs_function name="fcs/collective-cmd-norm-variant">
      <function>
        <sum>
          <!-- regular collective input if 'collective-profile' is 0 -->
          <product>
            <property> fcs/collective-cmd-norm </property>
            <difference>
              <value> 1.0 </value>
              <property> fcs/adj/collective-profile </property>
            </difference>
          </product>
          <!-- mapped input if 'collective-profile' is 1 -->
          <product>
            <property> fcs/adj/collective-profile </property>
            <property> fcs/misc/collective-cmd-norm-polymod </property>
          </product>
        </sum>
      </function>
    </fcs_function>
    <summer name="fcs/collective-cmd-trim-sum">
      <input> fcs/collective-cmd-norm-variant </input>
      <input> fcs/collective-trim-cmd-norm </input>
      <clipto>
        <min> -1.0 </min>
        <max>  1.0 </max>
      </clipto>
    </summer>
    <pure_gain name="fcs/collective-gain">
      <input>  fcs/collective-cmd-trim-sum </input>
      <gain>   fcs/adj/collective-gain </gain>
    </pure_gain>
    <summer name="fcs/collective-ctrl-rad">
      <input> fcs/collective-gain </input>
      <input> ap/collective-cmd </input>
      <input> fcs/adj/collective-bias </input>
      <output> fcs/collective-ctrl-rad </output>
    </summer>
    <lag_filter name="fcs/collective-ctrl-rad-lag">
      <input> fcs/collective-ctrl-rad </input>
      <!-- c1 ranges: 5.0 - 20.0 -->
      <c1>   12.0 </c1>
      <clipto>
        <min> 0.0 </min>
        <max> 0.8 </max>
      </clipto>
      <output> propulsion/engine[0]/collective-ctrl-rad </output>
    </lag_filter>

  </channel>

  <!-- - - - - - - - - - - - - - - - - -
          longitudinal
       - - - - - - - - - - - - - - - - - -->
  <property value='0.15'> fcs/adj/longitudinal-gain </property>
  <property value='0.025'> fcs/adj/longitudinal-bias </property>

  <channel name="longitudinal">

    <fcs_function name="fcs/elevator-cmd-norm-exmod">
      <description>
        Provide higher sensitivity when near center.
        new_input = sgn(input) * abs(input)**sensitivity,
        with sensitivity ranging between 1..2 .
      </description>
      <function>
        <product>
          <sign> <property> fcs/elevator-cmd-norm </property> </sign>
          <pow>
            <abs> <property> fcs/elevator-cmd-norm </property> </abs>
            <property> fcs/adj/center-sensitivity </property>
          </pow>
        </product>
      </function>
    </fcs_function>
    <summer name="fcs/longitudinal-cmd-trim-sum">
      <input> fcs/elevator-cmd-norm-exmod </input>
      <input> fcs/pitch-trim-cmd-norm </input>
      <clipto>
        <min> -1.0 </min>
        <max>  1.0 </max>
      </clipto>
    </summer>
    <pure_gain name="fcs/longitudinal-gain">
      <input>  fcs/longitudinal-cmd-trim-sum </input>
      <gain>   fcs/adj/longitudinal-gain </gain>
    </pure_gain>
    <summer name="fcs/longitudinal-ctrl-rad">
      <input> fcs/longitudinal-gain </input>
      <input> fcs/adj/longitudinal-bias </input>
      <input> ap/elevator-cmd </input>
      <clipto>
        <min> -0.8 </min>
        <max>  0.8 </max>
      </clipto>
    </summer>
    <lag_filter name="fcs/longitudinal-ctrl-rad-lag">
      <input> fcs/longitudinal-ctrl-rad </input>
      <!-- c1 ranges: 10.0 - 50.0 -->
      <c1>   25.0 </c1>
      <output> propulsion/engine[0]/longitudinal-ctrl-rad </output>
    </lag_filter>

  </channel>

  <!-- - - - - - - - - - - - - - - - - -
          lateral
       - - - - - - - - - - - - - - - - - -->
  <property value='0.06'> fcs/adj/lateral-gain </property>
  <property value='0.00'> fcs/adj/lateral-bias </property>

  <property value='1.00'> fcs/adj/lateral-col-mix-infl </property>
  <property value='-0.12'> fcs/adj/lateral-col-mix-gain </property>
  <property value='0.015'> fcs/adj/lateral-col-mix-bias </property>

  <channel name="lateral">

    <fcs_function name="fcs/aileron-cmd-norm-exmod">
      <description> See longitudinal channel. </description>
      <function>
        <product>
          <sign> <property> fcs/aileron-cmd-norm </property> </sign>
          <pow>
            <abs> <property> fcs/aileron-cmd-norm </property> </abs>
            <property> fcs/adj/center-sensitivity </property>
          </pow>
        </product>
      </function>
    </fcs_function>
    <summer name="fcs/lateral-cmd-trim-sum">
      <input> fcs/aileron-cmd-norm-exmod </input>
      <input> fcs/roll-trim-cmd-norm </input>
      <clipto>
        <min> -1.0 </min>
        <max>  1.0 </max>
      </clipto>
    </summer>
    <pure_gain name="fcs/lateral-gain">
      <input>  fcs/lateral-cmd-trim-sum </input>
      <gain>   fcs/adj/lateral-gain </gain>
    </pure_gain>
    <summer name="fcs/lateral-sum">
      <input> fcs/lateral-gain </input>
      <input> ap/aileron-cmd </input>
      <input> fcs/adj/lateral-bias </input>
      <clipto>
        <min> -0.8 </min>
        <max>  0.8 </max>
      </clipto>
    </summer>

    <!-- collective compensation -->
    <fcs_function name="fcs/lateral-ctrl-rad">
      <function>
        <sum>
          <property> fcs/lateral-sum </property>
          <product>
            <property> fcs/adj/lateral-col-mix-infl </property>
            <sum>
              <product>
                <property> fcs/collective-ctrl-rad </property>
                <property> fcs/adj/lateral-col-mix-gain </property>
              </product>
              <property> fcs/adj/lateral-col-mix-bias </property>
            </sum>
          </product>
        </sum>
      </function>
    </fcs_function>

    <lag_filter name="fcs/lateral-ctrl-rad-lag">
      <input> fcs/lateral-ctrl-rad </input>
      <!-- c1 ranges: 10.0 - 50.0 -->
      <c1>   25.0 </c1>
      <output> propulsion/engine[0]/lateral-ctrl-rad </output>
    </lag_filter>

  </channel>

  <!-- - - - - - - - - - - - - - - - - -
          pedal/antitorque
       - - - - - - - - - - - - - - - - - -->
  <property value='0.20'> fcs/adj/pedal-gain </property>
  <property value='0.00'> fcs/adj/pedal-bias </property>

  <property value='1.00'> fcs/adj/pedal-col-mix-infl </property>
  <!-- Antitorque gain history (collective-position feedforward, now
       replaced by a torque feedforward, see fcs/adj/pedal-torque-mix-
       gain below): 0.70 (matched to the old single mocked engine's
       ~5000 ft-lb reference torque) -> 3.92 (~5.6x, reasoning from a
       ~28000 ft-lb figure that turned out to be an artifact of the RPM
       governor not being wired to throttle) -> reverted to 0.70
       (reasoning from a ~5000-5800 ft-lb ground-idle torque figure
       that was itself wrong, since engine power was still an
       underpowered 1000 HP/side placeholder at the time, so the
       aircraft could not lift off at all) -> 7.5 (scaled from real
       hover torque, ~54000-56000 ft-lb combined, against the original
       5000 ft-lb reference). Each of those was a single gain on
       collective position, and none of them could be right across both
       idle and hover once the governor started actively regulating
       power: 7.5 alone gave ground-idle collective (bias-only, ~0.15
       rad) a mix term of 0.15*7.5-0.12 = 1.005 rad, clamped to the
       tail rotor's own 0.45 rad ceiling, commanding max tail rotor
       pitch while just sitting on the gear with next to no real torque
       to counter, and crashing on the initial ground settle. -->
  <!-- Switched from a collective-POSITION feedforward to a TORQUE
       feedforward: no single gain on collective can be right across
       both idle and hover once the RPM governor is actively regulating
       power, because torque doesn't scale simply with commanded
       collective any more (it depends on however much throttle the
       governor happens to be running at for the current RPM error).
       Concretely: at gain=7.5 (sized for real hover torque, see below)
       even ground-idle collective (bias-only, ~0.15 rad) already gave
       mix_term = 0.15*7.5-0.12 = 1.005 rad, clamped to the tail rotor's
       own 0.45 rad ceiling, commanding max tail rotor pitch while the
       aircraft was just sitting on its gear with next to no real
       torque to counter, and crashing on the initial ground settle.
       Reading actual combined main rotor torque instead scales
       correctly in any regime automatically: near-zero at idle,
       proportionally large at hover, without needing to pick one gain
       that's only right for one operating point.
       gain sized from real hover data (scripts/test_mi17_hover.xml):
       propulsion/engine[0]/torque-lbsft settles around 27000-28000
       ft-lb at hover (this IS the combined main rotor reaction torque
       already; FGRotor's own aerodynamic torque is a single value
       for the one shared rotor, tied only at the gearbox's lowest
       engine index per FGGearbox's convention, not duplicated per
       engine: propulsion/engine[1]/torque-lbsft does not exist).
       Solving so 27500 ft-lb maps to right at the tail rotor's own
       0.45 rad pitch ceiling (full available authority at the real
       worst-case load) gives gain = (0.45 - bias) / 27500 ~= 0.0000207
       rad per ft-lb. -->
  <property value='0.0000207'> fcs/adj/pedal-torque-mix-gain </property>
  <property value='-0.12'> fcs/adj/pedal-col-mix-bias </property>

  <channel name="pedal">

    <fcs_function name="fcs/rudder-cmd-norm-exmod">
      <description> See longitudinal channel. </description>
      <function>
        <product>
          <sign> <property> fcs/rudder-cmd-norm </property> </sign>
          <pow>
            <abs> <property> fcs/rudder-cmd-norm </property> </abs>
            <property> fcs/adj/center-sensitivity </property>
          </pow>
        </product>
      </function>
    </fcs_function>
    <summer name="fcs/pedal-cmd-trim-sum">
      <input> fcs/rudder-cmd-norm-exmod </input>
      <input> fcs/yaw-trim-cmd-norm </input>
      <clipto>
        <min> -1.0 </min>
        <max>  1.0 </max>
      </clipto>
    </summer>
    <pure_gain name="fcs/pedal-gain">
      <input>  fcs/pedal-cmd-trim-sum </input>
      <gain>   fcs/adj/pedal-gain </gain>
    </pure_gain>
    <summer name="fcs/pedal-sum">
      <input> fcs/pedal-gain </input>
      <input> fcs/adj/pedal-bias </input>
      <input> ap/rudder-cmd </input>
    </summer>

    <!-- torque compensation: main rotor reaction torque, not
         collective position; see fcs/adj/pedal-torque-mix-gain's
         comment above for why. propulsion/engine[0]/torque-lbsft is
         already the full rotor torque (a single physical value for the
         one shared rotor, not per-engine); no summing needed. -->
    <fcs_function name="fcs/pedal-ctrl-rad">
      <function>
        <sum>
          <property> fcs/pedal-sum </property>
          <product>
            <property> fcs/adj/pedal-col-mix-infl </property>
            <sum>
              <product>
                <property> propulsion/engine[0]/torque-lbsft </property>
                <property> fcs/adj/pedal-torque-mix-gain </property>
              </product>
              <property> fcs/adj/pedal-col-mix-bias </property>
            </sum>
          </product>
        </sum>
      </function>
    </fcs_function>

    <lag_filter name="fcs/pedal-ctrl-rad-lag">
      <input> fcs/pedal-ctrl-rad </input>
      <!-- c1 ranges: 10.0 - 50.0 -->
      <c1>   25.0 </c1>
      <!-- Unlike fcs/collective-ctrl-rad-lag below, this chain had no
           output clamp at all. With the mix gain raised (see the
           antitorque gain history comment above) plus the yaw-rate SAS term
           summed in via ap/rudder-cmd, an unclamped pedal-ctrl-rad was
           observed commanding ~0.9-1.0 rad (52-59 deg) of tail rotor
           blade pitch, more than double any physically real tail
           rotor collective range, and self-reinforcing (more commanded
           pitch drives more tail rotor shaft torque/reaction, which
           drives more yaw rate, which drives more SAS correction).
           Clamp to a plausible physical tail rotor pitch range, same
           pattern as the main rotor's collective clamp.

           Investigated (not just tuned) tail rotor authority before
           touching this: propulsion/engine[2]/rotor-rpm tracks its
           real 1200 RPM nominal correctly (confirms the gearing is
           right), and propulsion/engine[2]/thrust-lbs at this clamp
           fluctuates 590-1178 lbs against the ~853 lbs (torque/arm =
           ~27000 ft-lb / 31.67 ft, tail rotor at x=580in vs CG
           x=200in) needed to fully cancel real hover torque; i.e.
           genuinely close, not fundamentally undersized. Tried raising
           max to 0.55 for more SAS headroom; that made BOTH the
           residual yaw rate (-6.7 -> -7.7 rad/sec) and roll (~0.2-0.3
           -> ~0.4 rad) worse, not better, an unambiguous sign that more
           commanded tail rotor pitch is adding a disturbance rather
           than just providing more antitorque authority. Reverted to
           0.45; points at the tail rotor's own reaction torque (its
           <sense> defaults to the same CCW as the main rotor, since
           neither rotor's XML specifies it) rather than the clamp
           itself, see mi17_tail_rotor.xml. -->
      <clipto>
        <min> -0.26 </min>
        <max>  0.45 </max>
      </clipto>
      <!-- mind: tail rotor is on engine 1 -->
      <output> propulsion/engine[2]/antitorque-ctrl-rad </output>
    </lag_filter>

  </channel>

</system>
